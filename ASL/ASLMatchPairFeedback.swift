import Foundation

enum ASLMatchPairFeedback {
    /// Green flash before a matched pair is removed from the board.
    static let correctFlashDuration: TimeInterval = 0.35
    static let wrongFlashDuration: TimeInterval = 0.42
}
